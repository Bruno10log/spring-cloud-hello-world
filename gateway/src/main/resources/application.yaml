spring:
  application:
    name: gateway
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  cloud:
    config:
      name: api-gateway
  config:
    import: "optional:configserver:http://localhost:8888"